events {
    worker_connections 1024;
}

http {
    server {
        listen 80;

        location /api/ordenes {
            proxy_pass http://orden-service:8084;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
