events {
    worker_connections 1024;
}

http {
    server {
        listen 80;

        location /api/estados {
            proxy_pass http://estado-service:8082;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
