events {
    worker_connections 1024;
}

http {
    server {
        listen 80;

        location /api/roles {
            proxy_pass http://rol-service:8081;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
