events {
    worker_connections 1024;
}

http {
    server {
        listen 80;

        location /api/productos {
            proxy_pass http://producto-service:8085;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
