name: CI — SmartLogix Microservices

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [main]

jobs:

  # ── Diagnóstico (ver qué falla) ───────────────────────────────────────────────
  diagnostico:
    name: Diagnostico
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Entorno
        run: |
          java -version
          mvn -version
          ls -la
          ls Gateway/ | head -5

  # ── Gateway ──────────────────────────────────────────────────────────────────
  test-gateway:
    name: Gateway
    runs-on: ubuntu-latest
    needs: diagnostico
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Test
        run: mvn test -B --no-transfer-progress -s $GITHUB_WORKSPACE/maven-settings.xml
        working-directory: Gateway
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-gateway
          path: Gateway/target/site/jacoco/

  # ── Users ─────────────────────────────────────────────────────────────────────
  test-users:
    name: Users
    runs-on: ubuntu-latest
    needs: diagnostico
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Test
        run: mvn test -B --no-transfer-progress -s $GITHUB_WORKSPACE/maven-settings.xml
        working-directory: Users
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-users
          path: Users/target/site/jacoco/

  # ── Rol ───────────────────────────────────────────────────────────────────────
  test-rol:
    name: Rol
    runs-on: ubuntu-latest
    needs: diagnostico
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Test
        run: mvn test -B --no-transfer-progress -s $GITHUB_WORKSPACE/maven-settings.xml
        working-directory: Rol
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-rol
          path: Rol/target/site/jacoco/

  # ── Estado ────────────────────────────────────────────────────────────────────
  test-estado:
    name: Estado
    runs-on: ubuntu-latest
    needs: diagnostico
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Test
        run: mvn test -B --no-transfer-progress -s $GITHUB_WORKSPACE/maven-settings.xml
        working-directory: Estado
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-estado
          path: Estado/target/site/jacoco/

  # ── Inventario ────────────────────────────────────────────────────────────────
  test-inventario:
    name: Inventario
    runs-on: ubuntu-latest
    needs: diagnostico
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Test
        run: mvn test -B --no-transfer-progress -s $GITHUB_WORKSPACE/maven-settings.xml
        working-directory: Inventario
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-inventario
          path: Inventario/target/site/jacoco/

  # ── Producto ──────────────────────────────────────────────────────────────────
  test-producto:
    name: Producto
    runs-on: ubuntu-latest
    needs: diagnostico
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Test
        run: mvn test -B --no-transfer-progress -s $GITHUB_WORKSPACE/maven-settings.xml
        working-directory: Producto
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-producto
          path: Producto/target/site/jacoco/

  # ── Orden ─────────────────────────────────────────────────────────────────────
  test-orden:
    name: Orden
    runs-on: ubuntu-latest
    needs: diagnostico
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Test
        run: mvn test -B --no-transfer-progress -s $GITHUB_WORKSPACE/maven-settings.xml
        working-directory: Orden
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-orden
          path: Orden/target/site/jacoco/

  # ── Quality Gate ──────────────────────────────────────────────────────────────
  quality-gate:
    name: Quality Gate
    runs-on: ubuntu-latest
    needs: [test-gateway, test-users, test-rol, test-estado, test-inventario, test-producto, test-orden]
    steps:
      - name: Resultado
        run: |
          echo "✅ Gateway   — OK"
          echo "✅ Users      — OK"
          echo "✅ Rol        — OK"
          echo "✅ Estado     — OK"
          echo "✅ Inventario — OK"
          echo "✅ Producto   — OK"
          echo "✅ Orden      — OK"
          echo "Quality Gate PASSED"
